// Firebase configuration scaffold
// Replace with your actual Firebase config when ready to connect

const firebaseConfig = {
  apiKey: 'YOUR_API_KEY',
  authDomain: 'fitmaster-pro.firebaseapp.com',
  projectId: 'fitmaster-pro',
  storageBucket: 'fitmaster-pro.appspot.com',
  messagingSenderId: 'YOUR_SENDER_ID',
  appId: 'YOUR_APP_ID',
  measurementId: 'YOUR_MEASUREMENT_ID',
};

// Firebase will be initialized when the actual SDK is integrated
// For now, the app uses local Zustand stores with AsyncStorage persistence

export default firebaseConfig;
